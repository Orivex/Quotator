"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.repeatFunctionNTimes = exports.delay = exports.countMatchingCharacters = void 0;
/**
 * Delay function to pause execution for a specified time.
 * @param ms - The delay duration in milliseconds.
 */
const delay = ms => {
  return new Promise(resolve => {
    setTimeout(resolve, ms);
  });
};

/**
 * Repeat a function a specified number of times.
 * @param func - The function to repeat.
 * @param times - The number of times to repeat the function.
 */
exports.delay = delay;
const repeatFunctionNTimes = async (func, times) => {
  for (let i = 0; i < times; i++) {
    await func();
  }
};

/**
 * Count the matching characters between two strings.
 * @param text1 - The first string.
 * @param text2 - The second string.
 * @returns The count of matching characters.
 */
exports.repeatFunctionNTimes = repeatFunctionNTimes;
const countMatchingCharacters = (text1, text2, direction) => {
  let count = 0;
  const minLength = Math.min(text1.length, text2.length);
  if (direction === 'front') {
    for (let i = 0; i < minLength; i++) {
      if (text1[i] === text2[i]) {
        count++;
      } else {
        break;
      }
    }
  } else if (direction === 'back') {
    for (let i = 1; i <= minLength; i++) {
      if (text1[text1.length - i] === text2[text2.length - i]) {
        count++;
      } else {
        break;
      }
    }
  }
  return count;
};
exports.countMatchingCharacters = countMatchingCharacters;
//# sourceMappingURL=helper_functions.js.map